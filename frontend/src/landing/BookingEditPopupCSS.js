import color from "../general/colorScheme";

/**
 * Stores the stylesheet for booking edit popup components
 * @type {{}}
 */
const css = {
  button: {
    color: color.primary,
  },

};

export default css;
